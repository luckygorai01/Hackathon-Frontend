Place the Kaggle mirror CSV here as `all_india_pincode_directory_2025.csv`.

Expected columns include pincode, office name, district, state, latitude, and longitude. The backend will normalize column names and accept common variants such as `statename`, `office_name`, `pin_code`, `lat`, and `lon`.
